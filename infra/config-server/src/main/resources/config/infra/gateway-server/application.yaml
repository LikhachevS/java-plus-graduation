spring:
  application:
    name: gateway-server

  cloud:
    gateway:
      discovery.locator.enabled: true
      routes:
        - id: user-service-route
          uri: lb://user-service
          predicates:
              - Path=/admin/users/**
        - id: main-service-all-routes
          uri: lb://main-service
          predicates:
            - Path=/**